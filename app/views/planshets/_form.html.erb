<%= form_with(model: planshet) do |form| %>
  <% if planshet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planshet.errors.count, "error") %> prohibited this planshet from being saved:</h2>

      <ul>
        <% planshet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :gadget_id %>
    <%= form.text_field :gadget_id %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
